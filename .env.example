# CallTrackingMetrics API (same values as the legacy repo's .env.local)
CTM_API_HOST=https://api.calltrackingmetrics.com
CTM_ACCESS_KEY=
CTM_SECRET_KEY=
CTM_ACCOUNT_ID=

# Neon. Use the -pooler host for anything serverless; direct host is fine for
# the sync job (one long-lived connection per run).
DATABASE_URL=
